package tr.com.jowl.logback;

/*

import ch.qos.logback.classic.Logger;
import org.slf4j.LoggerFactory;

public class LogBackApp {
    private static Logger logger = (Logger) LoggerFactory.getLogger(LogBackApp.class);

    public static void main(String[] args) {
        logger.debug("Debug logback log message");
        logger.info("Info logback log message");
        logger.error("Error logback log message");

        try {
            badMethod();
        } catch (Exception e) {
            logger.error("Something happen{}", e.getMessage());
        }
    }

    public static void badMethod() throws Exception {
        throw new Exception("Im bad method");
    }

}
*/
